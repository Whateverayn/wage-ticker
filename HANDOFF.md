# 勤務リアルタイム給料カウンター — 引き継ぎ資料

## 概要

出勤時刻・退勤予定時刻・時給を入力すると、経過時間・進捗率・稼いだ金額をリアルタイムで表示する個人用Webツール。チャット上でHTML単一ファイルのプロトタイプ(`prototype.html`)を試作済み。ここからちゃんとしたプロジェクトとして作り直す。

サーバー不要、クライアントサイド完結のアプリという前提。

## 機能要件

### 基本機能
- 入力: 勤務開始時刻、勤務終了予定時刻、基本時給
- リアルタイム表示: 経過時間、進捗率(%)、稼いだ金額
- 更新頻度は高め(プロトタイプでは50ms間隔)
- 終業予定時刻を過ぎても計算を止めず、残業分も通常通り加算し続ける(サービス残業にしない)。終業予定を過ぎたら「残り時間」表示は「残業時間」表示に切り替える

### 割増時間帯
- 複数の時間帯を設定可能(例: 夜間、搬入出時間帯)
- 各時間帯ごとに割増方式を選択できる:
  - 固定額加算(例: +100円/時)
  - 割合増し(例: 25%増し)
- 日をまたぐ時間帯(22:00〜翌5:00など)に対応
- 複数の時間帯が重なる場合の挙動は要件として明示確認されていない。プロトタイプでは加算方式(両方適用)で実装したのみの暫定解釈

### 固定費
- 交通費のような、時間に関係ない固定額を複数追加できる
- 合計金額に含めて表示し、内訳(時給分/割増分/固定費)も表示する

### 永続化
- ブラウザの`localStorage`に自動保存し、再度開いたら自動的に再開
- `localStorage`が使えない/信頼できない環境向けに、現在の設定をプレーンテキスト(JSON)として書き出し・読み込みできる機能:
  - クリップボードへコピー(Clipboard API、失敗時は`execCommand('copy')`にフォールバック)
  - Web Share API でのテキスト共有
  - テキストエリアへの貼り付けからの読み込み

### 画像共有
- 現在の稼ぎ状況(金額・進捗・経過/残業時間)を1枚の画像として共有できる
- この画像だけはデザインを派手にしてよい(メインUIとは別デザインでよい、という指定)
- 共有方法:
  - `navigator.share`が画像ファイル共有に対応していればそれを使う
  - 使えない場合は、小さいサイズ(180px角程度)でプレビュー画像を表示し、長押し保存を促す
  - 自動ダウンロードリンク方式は不採用(非公開環境で機能しないため)

### デザイン
- 完全なダークテーマ(ライト/ダーク中間のような曖昧な配色にしない)
- メインUIはシンプル・ミニマルに
- 数字表示(金額・時間・進捗率)は桁がガタつかないよう等幅数字スタイル(tabular-nums)を必ず当てる
- 金額は小数点以下2桁固定表示、進捗率は小数点以下3桁表示
- input(特に時刻入力)が画面幅からはみ出さないよう注意すること(プロトタイプで一度発生した不具合)

## プロトタイプについて

`prototype.html` はチャット上での試作(4イテレーション目)。素のHTML/CSS/バニラJS単一ファイル、ビルドツールなし。動作リファレンスとして使えるが、そのまま拡張する前提ではなく、ちゃんとした構成で作り直すことを想定。

技術的に参考になる実装ポイント:
- 割増時間帯の重なり計算は「日ごとにその時間帯を実時刻に変換し、経過区間との重なりを積算する」方式(`bandOverlapMs`関数)。複数日にまたがるセッションでは日数分ループする素朴な実装で、長時間セッションでの効率は未検証
- Claude.aiのアーティファクトプレビュー環境内では`localStorage`が機能しない場合がある(サンドボックス起因)。実際にホスティングして使う分には問題ない想定

## 検討の余地がある点(要相談 or 実装判断)

- PWA化(ホーム画面追加・オフライン対応)は明示的な要望ではないが、用途的に相性が良さそう
- 複数セッション(勤務日ごとの履歴)を残すかどうかは未決定。現状は「今の1回の勤務」だけを追跡する設計
- 割増時間帯が重なった場合の加算方式は要件として確認されたわけではない
